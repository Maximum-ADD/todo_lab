# Todo Lab

A local-first todo application built with Next.js and SQLite. See `docs/running-it.md`
for exact setup and run instructions, `docs/database-design.md` for the schema, and
`docs/third-party-code.md` for dependencies.

## AI Usage

This repository makes use of AI code generation using the following tools:
Claude-Web[Claude Sonnet 5].

This repository makes use of AI in-line editing using the following tools:
<fill in, or state "None">.

This repository does not use AI code review.

Full conversation transcripts are indexed in `transcripts/links_to_convos.md`.

This README was generated with the assistince of Claude-Web(Sonnet 5)
